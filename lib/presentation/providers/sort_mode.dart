/// User-selectable ordering for the home list.
enum SortMode {
  /// Future items first (soonest upcoming), then past (most recent first).
  closestUpcoming,

  /// Past items first (most recent), then future (soonest first).
  mostRecentPast,
}
